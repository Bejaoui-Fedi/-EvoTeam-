<form method="post" action="{{ path('app_wellbeing_tracker_delete', {'id': wellbeing_tracker.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');" style="display:inline;">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ wellbeing_tracker.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
